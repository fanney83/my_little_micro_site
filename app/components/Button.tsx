'use client'

export const Button = ({}) => {
  return (
    <button
      type="submit"
      className={`p-2 rounded-xl "${'border-2 border-purple-400 '} `}
    >
      Add todo
    </button>
  )
}
